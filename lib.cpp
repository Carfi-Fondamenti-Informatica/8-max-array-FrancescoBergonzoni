#include <iostream>
#include "lib.h"
using namespace std;

float funzione(const int dimensione, float numeri[dimensione]){
   numeri[dimensione];
   float max=num[0];
   for(int i=0, i<dimensione, i++){
       if(numeri[1]>max){
           max=numeri[1];
       }
   }
   return max;
}
